export class Category {
    constructor(id: number, name: string, color: string) {
        this.id = id;
        this.name = name;
        this.color = color;
    }
    id: number;
    name: string;
    color: string;
}
